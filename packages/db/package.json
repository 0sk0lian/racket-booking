{
  "name": "@racket-booking/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'no build step for db'",
    "db:migrate": "node migrate.js"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "dotenv": "^16.4.0"
  }
}
